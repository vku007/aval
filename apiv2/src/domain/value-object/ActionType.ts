/**
 * ActionType enum represents the type of action in a game.
 * Surrender - Player surrenders the game
 * Move - Player makes a move
 */
export enum ActionType {
  Surrender = 'Surrender',
  Move = 'Move'
}

